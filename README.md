# RoleConnectors

Maintained by Luna Hook.

RoleConnectors links Minecraft and Discord roles.

Build with Java 21 and the included Gradle wrapper: `./gradlew build`. Keep bot tokens, guild IDs, and other deployment values in local configuration only.
